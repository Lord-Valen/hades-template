{
  "name": "hades-template",
  "description": "A template for making bots with the Hades framework using Typescript and Inversify",
  "version": "0.0.1",
  "author": "Lord-Valen",
  "licence": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf ./dist; tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json -w",
    "test": "jest --coverage",
    "test:watch": "jest --watch --verbose"
  },
  "dependencies": {
    "hades": "github:dustinlacewell/hades",
    "inversify": "^6.0.1",
    "reflect-metadata": "^0.1.13",
    "sqlite3": "^5.1.5"
  },
  "devDependencies": {
    "@types/jest": "^28.1.3",
    "jest": "^28.1.1",
    "ts-jest": "^28.0.5",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.4"
  }
}
